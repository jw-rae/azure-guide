{
  "name": "azure-review-site",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "generate-data": "node scripts/generate-data.mjs",
    "dev": "node scripts/generate-data.mjs && vite",
    "build": "node scripts/generate-data.mjs && vite build",
    "preview": "vite preview",
    "deploy": "node scripts/deploy.mjs"
  },
  "dependencies": {
    "@jwrae/design-tokens": "^1.0.3",
    "fuse.js": "^7.4.2",
    "marked": "^18.0.5",
    "vue": "^3.5.34",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.6",
    "vite": "^8.0.12"
  }
}
